# حالة المشروع — مؤسسة حلم دمشقي

آخر تحديث: 2026-08-15

> **هذا الملف هو المرجع الوحيد لأي جلسة عمل قادمة.** اقرأه بالكامل قبل البدء بأي تعديل — يشرح ماذا بُني، لماذا بهذا الشكل تحديداً، وأين العقبات المعروفة.

---

## 1. الأساس (i18n)
- **Next.js 16.2.10 + Tailwind v4 + next-intl 4.13.2**، مسارات عربي/إنجليزي تحت `app/[locale]/`.
- `proxy.ts` (بديل `middleware` المُهمَل في Next 16) يكتشف اللغة ويوجّه `/` → `/ar` (الافتراضية) أو `/en`.
- `i18n/routing.ts` + `navigation.ts` + `request.ts`، والرسائل في `messages/ar.json` و`en.json`.
- `LayoutProps<"/[locale]">` مع `params` كـ`Promise` (تغيير جوهري في Next 16).
- [components/LocaleSwitcher.tsx](components/LocaleSwitcher.tsx) يبدّل اللغة مع حفظ المسار، ويقلب `dir="rtl/ltr"` على `<html>`.
- **AGENTS.md يحذّر بوضوح**: هذا إصدار Next.js "غير الذي تعرفه" — راجع `node_modules/next/dist/docs/` قبل أي كود جديد يعتمد سلوكاً مألوفاً من إصدارات أقدم.

## 2. الثيم
ثلاثة ألوان في `app/globals.css` (`@theme inline` — طريقة Tailwind v4):
- **`ink`** `#1A1410` (أسود دافئ) — الخلفيات.
- **`ivory`** `#EDE4D3` (عاجي) — خلفيات البطاقات/الأزرار الفاتحة.
- **`gold`** `#C9A24B` — التمييز (عناوين، حدود، شارات).

## 3. الخطوط (Amiri عربي / Cormorant Garamond إنجليزي)
- [app/fonts.ts](app/fonts.ts): تحميل عبر `next/font/google`. التبديل حسب اللغة عبر متغيّر CSS واحد (`--font-body`، يتبدّل بمحدِّد `html[lang="ar"]`) — فيُطبَّق تلقائياً بلا تكرار بالمكوّنات.
- **فخ معروف**: `--font-sans: var(--font-sans)` داخل `@theme inline` مرجع ذاتي دائري يُسقط الخط بصمت. استخدم اسمين مختلفين دائماً (`--font-body` يُستهلَك من `--font-sans`، لا العكس).

## 4. قاعدة البيانات
- **Prisma 7.8.0** — مولّد `prisma-client` (لا `prisma-client-js`)، `output` إلزامي، `prisma.config.ts` إلزامي لتحميل متغيّرات البيئة يدوياً (`.env.local` لا يُحمَّل تلقائياً).
- العميل المولَّد **لا يقبل `datasourceUrl`** — فقط `adapter` أو `accelerateUrl`. الاتصال دائماً: `new PrismaClient({ accelerateUrl: process.env.DATABASE_URL })`.
- **القاعدة الفعّالة الآن: Prisma Postgres على `us-east-1`** عبر Prisma Accelerate (`prisma+postgres://accelerate.prisma-data.net/?api_key=...` في `.env.local`).
  - ⚠️ **لا تُنشئ قاعدة جديدة بمنطقة `eu-central-1`** — كانت مصدر عطل حقيقي معلن من Prisma (`P6008`) استغرق تشخيصه وقتاً طويلاً. القاعدة الحالية بـ`us-east-1` سليمة ومستقرة.
  - ⚠️ **`npx create-db` يختار المنطقة حسب موقع IP الظاهري (VPN)** — إن أنشأت قاعدة جديدة يوماً، **أوقف الـVPN أو بدّله لموقع أمريكي أولاً**، وإلا قد تحصل على منطقة معطوبة أو بعيدة.
- **`ArchiveItem` هو النموذج الأكثر نشاطاً** — راجع [prisma/schema.prisma](prisma/schema.prisma) للحقول الكاملة. النماذج الستة: `ArchiveItem`, `Artisan`, `TrainingProgram`, `NewsPost`, `Partner`, `ContactMessage`.
- **الـmigrations حتى الآن** (بالترتيب):
  1. `20260715222309_init` — الجداول الستة الأساسية.
  2. `20260811223127_add_file_url_hd` — `fileUrlHd` (مصدر جودة أعلى اختياري لعناصر الفيديو، يُفعّل مبدّل SD/HD بالمشغّل).
  3. `20260814180000_replace_era_with_year` — **حذفت `era` (String) وأضافت `year` (Int?)**. الفلترة صارت بالسنة لا بالعصر.
  4. `20260815000000_add_thumbnail_url` — `thumbnailUrl` (اختياري) لصورة مصغّرة تُعرض بالشبكة بدل الصورة الكاملة.
- **`prisma/seed.ts` يمسح ويعيد بناء البيانات التجريبية فقط** (`deleteMany()` ثم 5 حرفيين + 14 عنصر أرشيف وهمي). **لا تُشغّله بعد إضافة بيانات حقيقية** — سيمحوها. للبيانات الحقيقية استخدم السكربتات المخصصة أدناه (إضافية، لا تمسح شيئاً).

## 5. الصفحة الرئيسية
- **Navbar**: شعار + 6 روابط أقسام + مبدّل اللغة. راجع القسم 10 لتفاصيل إصلاحات القياسات.
- **Hero**: صورة خلفية عبر `next/image` (`fill` + `preload` بدل `priority` المُهمَل) + تدرّج تعتيم + عنوان/وصف/زرّان.
  - **معلّق**: زرّا "استكشف رسالتنا" و"ادعم الحرفيين" بلا وجهة/وظيفة فعلية بعد — يحتاجان قراراً (حذف أو ربط بصفحات حقيقية).
- **نظرة عامة على الأقسام**: 5 بطاقات (بلا "الرئيسية"، ذاتية المرجع) بأيقونات SVG يدوية.
- **Footer**: اسم المؤسسة، حقوق نشر ديناميكية، أيقونات تواصل اجتماعي (لا تزال placeholder).

## 6. مركز التدريب
- `/training` (نبذة + 4 بطاقات) + `/training/damascene`, `/training/armenian` (تخصصات) + `/training/archive` (خط زمني 2016–2025) + `/training/2026` (وصف + مكوّنات + زر تحميل).
- **معلّق**: زر "تحميل دليل التدريب" بلا ملف PDF فعلي مرفق.
- كل المحتوى تحت namespace `training`، نصوص رسمية مختصرة مطبَّقة (راجع القسم 12).

## 7. التنقّل الداخلي (Breadcrumb)
- [components/Breadcrumb.tsx](components/Breadcrumb.tsx) نمط قياسي مطبَّق على كل صفحة فرعية: `about/*`, `training/*`, `contact`. يُطبَّق تلقائياً عند إضافة أي صفحة جديدة.

## 8. بنك الذاكرة — البنية والفلاتر

### الفلترة الحالية (بعد إعادة التصميم الأخيرة)
- **النوع** (`type`): `photo` | `document` | `audio` | `video` | `event` — قائمة بيضاء في [lib/archive-filters.ts](lib/archive-filters.ts) (`ARCHIVE_TYPES`).
- **السنة** (`year`): 2016–2025 تنازلياً (`ARCHIVE_YEARS`) — **يظهر فقط عند اختيار نوع "صورة"** (`YEAR_FILTER_TYPE = "photo"`). هذا شرط عرض بالواجهة فقط، لا قيد بالقاعدة — أي نوع آخر يمكن نظرياً أن يملك `year` لكن الفلتر لا يعرضه له.
- [components/MemoryFilterBar.tsx](components/MemoryFilterBar.tsx) صار **مكوّن عميل** (`"use client"`) لأن ظهور فلتر السنة يتبع اختيار النوع لحظياً قبل الإرسال — لا يزال يعمل عبر نموذج `GET` أصلي يحدّث `searchParams`، والصفحة (Server Component) تستعلم من القاعدة بشرط `WHERE` حقيقي مبني فقط من قيم مُتحقَّق منها.
- **`era` أُزيلت بالكامل** من الـschema والترجمة والفلاتر — إن وجدت أي إشارة لها ببقايا كود قديم فهي خطأ يجب حذفه.

### الأنواع الخمسة وسلوك كل منها بالنافذة (Modal)
راجع [components/ArchiveModal.tsx](components/ArchiveModal.tsx):
| النوع | السلوك بالنافذة |
|---|---|
| `photo` | أنيميشن **flip** ثلاثي الأبعاد (`rotateY` 180°، CSS 3D حقيقي بـ`perspective` + `backface-visibility`)، ثم الصورة الكاملة. |
| `document` | يُعامَل معاملة الصورة تماماً (`IMAGE_TYPES = Set(["photo", "document"])`) — وثائق ممسوحة ضوئياً، نفس الـflip. |
| `audio` | مشغّل `<audio controls>` قياسي، بسيط. |
| `video` | **بلا** أنيميشن دوران — ظهور متدرّج فقط (`fadeIn` keyframe في `globals.css`). يستخدم [components/VideoPlayer.tsx](components/VideoPlayer.tsx). |
| `event` (فعالية ومعرض) | **نفس منطق `video`** — ملفاتها mp4 حقيقية (`VIDEO_TYPES = Set(["video", "event"])` بداخل ArchiveModal). هذا كان عطلاً حقيقياً وقع فعلياً: إضافة نوع `event` بدون توسيع هذا الـSet جعلت الفيديوهات الأربعة الحقيقية تسقط في حالة "لا معاينة" رغم كونها ملفات صالحة. |

### مشغّل الفيديو المخصص ([components/VideoPlayer.tsx](components/VideoPlayer.tsx))
عناصر تحكم كاملة مبنية يدوياً (لا `controls` الافتراضية):
- تشغيل/إيقاف، شريط تقدّم، مستوى صوت (مخفي بالجوال، لا فائدة له مع أزرار الجهاز)، ملء الشاشة، **ومبدّل جودة SD/HD** يظهر تلقائياً فقط إن وُجد `fileUrlHd`.
- `dir="ltr"` مثبّت على شريط التحكّم بالذات (حتى بالعربية) — المحور الزمني يُقرأ يساراً→يميناً عالمياً بغضّ النظر عن لغة الواجهة.
- تبديل الجودة يحافظ على اللحظة الزمنية وحالة التشغيل (`restoreRef`) — لا يرجع الفيديو للبداية.
- **فخّ اكتُشف فعلياً**: تنظيف الأثر (`useEffect` cleanup) كان يستخدم `v.removeAttribute("src")` — في وضع Strict (تطوير) React يشغّل الأثر مرتين على نفس العقدة، فيمسح المصدر الحقيقي فعلياً ولا يعيد React ضبطه. الحل: الاكتفاء بـ`v.pause()` فقط بالتنظيف.
- `object-fit: contain` هو **افتراضي المتصفح لـ`<video>`** (بخلاف `<img>` الذي افتراضه `fill`) — فيديو عمودي داخل صندوق 16:9 لا يتشوّه تلقائياً بلا أي CSS إضافي (تحقّقنا تجريبياً، لم يكن بديهياً).

### النافذة بلا عنوان/وصف (حالة الصور الأرشيفية بلا بيانات وصفية)
- 415 صورة الأرشيف السنوي دخلت بـ`titleAr/titleEn/descriptionAr/descriptionEn` **فارغة عمداً**.
- `ArchiveModal` يتحقق من `item.title.trim().length > 0` (`hasTitle`) ويحذف كتلة العنوان بالكامل إن كانت فارغة — لا مساحة معلّقة، لا نص "بلا عنوان" بديل. النافذة تعرض **الصورة فقط**.
- للوصولية: حين لا يوجد عنوان مرئي، النافذة تحمل `aria-label` (غير مرئي) بدل `aria-labelledby` المعتاد.
- [components/ArchiveCard.tsx](components/ArchiveCard.tsx) يحذف بالمثل `<h3>` والوصف إن كانا فارغين، ويعرض **صورة مصغّرة (`thumbnailUrl`)** بدل صندوق فارغ متطابق — كانت هذه إضافة ضرورية بقرار: بلا عنوان ولا صورة كانت الشبكة ستكون 415 بطاقة متطابقة بلا أي فائدة عملية.

### أداء الشبكة
- **Lazy loading مثبَت رقمياً لا نظرياً**: على سنة فيها 116 عنصر، تحمّلت 12 صورة فقط لحظة فتح الصفحة (`loading="lazy"` على `<img>` بالبطاقة).
- الشبكة تستخدم `thumbnailUrl` (600px)، النافذة تفتح `fileUrl` الكامل (1920px) — منفصلان تماماً، لا تحميل مضاعف.

## 9. صفحة الأخبار (/news)
- **[app/[locale]/news/page.tsx](app/[locale]/news/page.tsx)**: 6 أخبار وهمية ثابتة بالكود، **بلا اتصال بـ`NewsPost`** رغم وجود النموذج بالقاعدة. معلّق للربط لاحقاً.

## 10. صفحة التواصل (/contact) — Resend
- [app/[locale]/contact/actions.ts](app/[locale]/contact/actions.ts) (Server Action) يرسل عبر `resend.emails.send()`. تحقّق `zod` بلغتين.
- **قيد الخطة المجانية**: الإرسال فقط من `onboarding@resend.dev` إلى **البريد المسجَّل بحساب Resend نفسه**. `CONTACT_EMAIL` بـ`.env.local` حالياً **بريد شخصي مؤقت** — ⚠️ **يجب تغييره لبريد المؤسسة الرسمي**، ومثالياً توثيق نطاق فعلي بـResend (مثل `contact@halmdimashqi.org`) لإلغاء هذا القيد كلياً.
- لا يُخزَّن شيء بـ`ContactMessage` بعد — الإرسال مباشر عبر Resend فقط.

## 11. النصوص الرسمية
- النصوص الرسمية المختصرة (سطر–سطرين، بأسلوب البطاقات القصيرة) مطبَّقة على: الرئيسية، من نحن وفروعها الثلاثة، مركز التدريب وفروعه الأربعة، بنك الذاكرة، تواصل معنا — بكلا `ar.json`/`en.json`.
- **معلّق**: "من نحن" وفروعها لا تزال بمحتوى **مختصر لكن عام** — تحتاج استبداله بمعلومات دقيقة وحقيقية عن المؤسسة (تاريخها، فريقها الفعلي، رؤيتها الموثّقة) حين تتوفر.

## 12. الوسائط الحقيقية المضافة (فيديوهات + صور)

### 4 فيديوهات "فعالية ومعرض" ([public/archive/events/](public/archive/events/))
- ملفات حقيقية (لا عيّنات تجريبية)، بأسماء نظيفة (بدون مسافات/عربي): `khan-asaad-pasha-exhibition.mp4`, `hawash-university-promo.mp4`, `hawa-shami-initiative.mp4`, `hawa-sham-initiative.mp4`.
- أُدخلت بـ[prisma/add-archive-events.ts](prisma/add-archive-events.ts) — **إضافي لا يمسح شيئاً**، ويتحقق من `titleEn` قبل الإدخال (يمكن تشغيله أكثر من مرة بأمان). للتشغيل: `npx tsx prisma/add-archive-events.ts`.
- العناوين/الأوصاف الحالية **مؤقتة بقرار المستخدم** — ستُستبدل لاحقاً بمعلومات دقيقة.
- **حجمان يتجاوزان حد GitHub الصارم (100MB)** — لذا فُعّل **Git LFS** لكل `*.mp4` تحت `public/archive/` (`.gitattributes`، `git lfs track "public/archive/**/*.mp4"`). ⚠️ **أي فيديو جديد يُضاف هناك يُغطّى تلقائياً — لا تضف فيديو كبير خارج هذا المسار بدون التحقق من تغطية LFS له.**

### 415 صورة أرشيف سنوي 2016–2024 ([public/archive/photos/[سنة]/](public/archive/photos/))
- المصدر الأصلي 415 صورة، 962MB. **حُسّنت عبر [scripts/optimize-archive-photos.mjs](scripts/optimize-archive-photos.mjs) (sharp) إلى ~102MB (توفير 89%)**:
  - نسخة عرض: أقصى عرض 1920px، جودة JPEG 82 → `public/archive/photos/[سنة]/[سنة]-NNN.jpg`.
  - نسخة مصغّرة: أقصى عرض 600px، جودة 78 → `public/archive/photos/[سنة]/thumbs/[سنة]-NNN.jpg`.
  - **`.rotate()` بلا وسائط مُطبَّق دائماً** — يقرأ دوران EXIF ويطبّقه فعلياً على البكسلات ثم يُسقط علم EXIF. **ضروري**: صور كاميرا/هاتف كثيرة مخزَّنة بعلم `orientation=6` (أفقية بالبيانات، عمودية بالعرض) — بدون هذا السطر كانت ستظهر مائلة 90° بالمتصفح.
  - إعادة التشغيل لأي سنة: `node scripts/optimize-archive-photos.mjs 2018` (أو عدة سنوات معاً).
- أُدخلت بـ[prisma/add-archive-photos.ts](prisma/add-archive-photos.ts) — يقرأ المجلد تلقائياً (لا قائمة يدوية)، **إضافي/idempotent**: يتحقق من `fileUrl` قبل الإدخال، ويحدّث `thumbnailUrl` للصفوف القديمة إن كانت ناقصة بدل تكرارها. للتشغيل: `npx tsx prisma/add-archive-photos.ts 2016 2017 2018 ...` (سنة أو أكثر بنفس الاستدعاء).
- **معلّق بقرار صريح من المستخدم**: كل الـ415 صورة بلا `titleAr/titleEn/descriptionAr/descriptionEn` — فارغة عمداً بانتظار تعبئتها لاحقاً بمعلومات حقيقية.
- العدد لكل سنة (مؤكَّد عبر الفلتر الحيّ): 2016=9، 2017=12، 2018=110، 2019=115، 2020=14، 2021=7، 2022=116، 2023=11، 2024=21. **لا توجد صور 2025 بالمصدر الحالي.**

## 13. Navbar — سلسلة إصلاحات القياسات والاتجاه
هذا القسم عانى من عدة جولات تصحيح متتالية — القيم أدناه هي **الحالة النهائية المستقرة**، لا تُرجعها لأي حالة وسيطة سابقة بالغلط:
- **rem → px بكل القياسات**: `html` بجذر خط مختلف بين اللغتين (Amiri/Cormorant) يجعل أي قياس بـ`rem` يتفاوت بصمت بين `/ar` و`/en`. كل قياسات Navbar وLocaleSwitcher الآن بكسل ثابت صراحة.
- **الشعار ومبدّل اللغة مثبَّتان فيزيائياً** (`dir="ltr"` على الحاوية الخارجية) — لا ينعكسان مع اتجاه الصفحة، بعكس بقية الموقع. طُلب هذا صراحة: "أريدهما بمكانهما دائماً متل الإنجليزي".
- **مسافة إضافية `pl-[48px]` للعربي فقط** على مجموعة الروابط — لضبط توازن المسافة البصرية بين الشعار والروابط بالعربي تحديداً (النص العربي أقصر مساحة من الإنجليزي بنفس الروابط).
- **عطل JSON/Turbopack محلول**: كان يظهر أحياناً `SyntaxError: Unexpected end of JSON input` عشوائياً بعد تعديلات Navbar. السبب: كاش Turbopack (`.next/`) يتعارض أحياناً مع تعديلات متكرّرة سريعة. الحل القياسي: **امسح `.next/` وأعد التشغيل** إن ظهر هذا العطل.

## 14. مشاكل بيئة معروفة ومتكرّرة (اقرأ قبل التشخيص من الصفر)
- **HMR متأخر أحياناً**، خاصة بعد تعديل CSS/خطوط/Navbar: إن لم يظهر تغييرك بالمتصفح رغم صحة الكود، **أعد تشغيل السيرفر كاملاً** (`Ctrl+C` ثم `npm run dev`) قبل افتراض وجود عطل بالكود.
- **عطل JSON غريب من Turbopack**: امسح `.next/` وأعد التشغيل (راجع القسم 13).
- **اختيار منطقة قاعدة بيانات جديدة يتأثر بالـVPN**: أوقفه أو بدّله لموقع أمريكي قبل `npx create-db` (راجع القسم 4).
- **Git LFS**: مفعّل حالياً فقط لـ`*.mp4` تحت `public/archive/`. الصور (`.jpg`) بـgit عادي — أحجامها بعد التحسين صغيرة بما يكفي (معدّل 218KB للعرض، 34KB للمصغّرة).

## 15. المتبقي (مرتّب بالأولوية)
- [ ] **إخفاء أدلة الـAI من الكود** (`AGENTS.md`, `.claude/`, إلخ) قبل أي نشر عام — بانتظار تقرير فحص شامل لتحديد كل ما يجب إخفاؤه أو استثناؤه من النشر.
- [ ] **Hero**: قرار بخصوص زرّي "استكشف رسالتنا" و"ادعم الحرفيين" (حذف أو ربط بوجهة حقيقية).
- [ ] **`CONTACT_EMAIL`**: تبديل من البريد الشخصي المؤقت لبريد المؤسسة الرسمي بـ`.env.local`.
- [ ] **"من نحن" وفروعها**: استبدال المحتوى العام الحالي بمعلومات حقيقية ودقيقة عن المؤسسة.
- [ ] **بنك الذاكرة — `document` و`audio`**: لا تزال بيانات تجريبية فقط (من `seed.ts`) — قرار مطلوب: إضافة محتوى حقيقي، أم إخفاء هذين النوعين مؤقتاً من الفلتر حتى يتوفر محتوى فعلي؟
- [ ] **عناوين/أوصاف الـ415 صورة**: فارغة عمداً — تحتاج تعبئة تدريجية بمعلومات دقيقة (لا نصوص عامة عشوائية).
- [ ] **PDF دليل التدريب**: زر "تحميل" بـ`/training/2026` بلا ملف فعلي مرفق.
- [ ] **ربط `/news` بـ`NewsPost` الحقيقي** بدل المحتوى الوهمي الثابت.
- [ ] **توثيق نطاق Resend**: إلغاء قيد `onboarding@resend.dev` بربط نطاق حقيقي (مثل `contact@...`).
- [ ] hover effects فخمة عبر الموقع (توهج ذهبي، انتقالات ناعمة) — مؤجّل بعد اكتمال الأقسام الوظيفية.
- [ ] إعادة تصميم قائمة الهمبرغر بصرياً (وظيفية حالياً، تصميمها بسيط).
- [ ] الأقسام الفرعية المتبقية لبنك الذاكرة (التراث المهدَّد، كنوز بشرية حية، إلخ) — إن كانت لا تزال مطلوبة ببنية منفصلة عن `ArchiveItem` الحالي.
- [ ] ربط `Artisan`, `TrainingProgram`, `Partner` بواجهات فعلية — جاهزة بالقاعدة، غير مستهلَكة بالواجهة بعد.
